
Project Setup Instructions

This project consists of frontend and backend files. Follow these steps:

Frontend:
Navigate to frontend folder.
Run npm i to install packages.
Execute npm start to launch the frontend.
UI will open in your browser automatically.



Backend:
Navigate to backend folder.
Run npm i to install packages.
Execute npm start to start the backend server.
Perform necessary checks on the UI.